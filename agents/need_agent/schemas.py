# Schemas for NeedAgent

# Placeholder structure for matching needs data
need_schema = {
    "type": "object",
    "properties": {
        "organization_id": {"type": "string"},
        "organization_name": {"type": "string"},
        "priority": {"type": "string"}
    }
}
